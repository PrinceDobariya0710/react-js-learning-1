import React from 'react'

const LearnProps = (props) => {
    // const LearnProps = ({name,role}) => {
  return (
    <div>
        <h1>All props</h1>
        <h1>name prop :{props.name}</h1>
        <h1>role prop :{props.role}</h1>
    </div>
  )
}

export default LearnProps